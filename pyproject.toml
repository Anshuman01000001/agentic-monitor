[project]
name = "slm_project"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["openai>=1.0.0"]

[project.scripts]
slm-chat = "slm_project.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]